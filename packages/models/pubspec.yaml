name: models
description: Shared data models and domain types for the Cos project
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  freezed_annotation: ^3.1.0
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.4.14
  freezed: ^3.2.5
  json_serializable: ^6.13.0
  very_good_analysis: ^10.2.0
