name: domain
description: Domain services and repositories for the API
version: 0.1.0+1
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  datasource:
    path: ../datasource
  injectable: ^2.7.1
  models:
    path: ../../../packages/models

dev_dependencies:
  build_runner: ^2.12.2
  injectable_generator: ^2.12.1
  very_good_analysis: ^10.2.0
